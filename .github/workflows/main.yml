on:
 push:
   branches:
     - master








jobs:
 bulid:
   runs-on: ubuntu-latest

   steps:
     - name: 'before'
       run: ls

     - uses: actions/checkout@v3  

     - name: 'printing folder contents'
       run: ls
   steps:
     - name: 'after'
       run: ls

   